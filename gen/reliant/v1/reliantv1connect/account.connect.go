// Copyright (c) 2025 Reliant Labs. All rights reserved.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: reliant/v1/account.proto

package reliantv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/reliant-labs/reliant/gen/reliant/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AccountServiceName is the fully-qualified name of the AccountService service.
	AccountServiceName = "reliant.v1.AccountService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AccountServicePreviewAccountDeletionProcedure is the fully-qualified name of the AccountService's
	// PreviewAccountDeletion RPC.
	AccountServicePreviewAccountDeletionProcedure = "/reliant.v1.AccountService/PreviewAccountDeletion"
	// AccountServiceDeleteAccountProcedure is the fully-qualified name of the AccountService's
	// DeleteAccount RPC.
	AccountServiceDeleteAccountProcedure = "/reliant.v1.AccountService/DeleteAccount"
)

// AccountServiceClient is a client for the reliant.v1.AccountService service.
type AccountServiceClient interface {
	// PreviewAccountDeletion reports what DeleteAccount would destroy, without
	// destroying anything. The UI shows these counts in the confirmation dialog
	// so the user sees the actual scale of what they are about to lose rather
	// than a generic warning.
	PreviewAccountDeletion(context.Context, *connect.Request[v1.PreviewAccountDeletionRequest]) (*connect.Response[v1.PreviewAccountDeletionResponse], error)
	// DeleteAccount permanently removes every row reliant holds for the caller.
	// Irreversible: there is no soft-delete and no undo.
	//
	// The caller must echo their own email address in confirm_email. This is a
	// deliberate speed bump on an unrecoverable action, and it is verified
	// server-side against the JWT's email claim — a client that skips its own
	// dialog still cannot delete by accident.
	DeleteAccount(context.Context, *connect.Request[v1.DeleteAccountRequest]) (*connect.Response[v1.DeleteAccountResponse], error)
}

// NewAccountServiceClient constructs a client for the reliant.v1.AccountService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAccountServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AccountServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	accountServiceMethods := v1.File_reliant_v1_account_proto.Services().ByName("AccountService").Methods()
	return &accountServiceClient{
		previewAccountDeletion: connect.NewClient[v1.PreviewAccountDeletionRequest, v1.PreviewAccountDeletionResponse](
			httpClient,
			baseURL+AccountServicePreviewAccountDeletionProcedure,
			connect.WithSchema(accountServiceMethods.ByName("PreviewAccountDeletion")),
			connect.WithClientOptions(opts...),
		),
		deleteAccount: connect.NewClient[v1.DeleteAccountRequest, v1.DeleteAccountResponse](
			httpClient,
			baseURL+AccountServiceDeleteAccountProcedure,
			connect.WithSchema(accountServiceMethods.ByName("DeleteAccount")),
			connect.WithClientOptions(opts...),
		),
	}
}

// accountServiceClient implements AccountServiceClient.
type accountServiceClient struct {
	previewAccountDeletion *connect.Client[v1.PreviewAccountDeletionRequest, v1.PreviewAccountDeletionResponse]
	deleteAccount          *connect.Client[v1.DeleteAccountRequest, v1.DeleteAccountResponse]
}

// PreviewAccountDeletion calls reliant.v1.AccountService.PreviewAccountDeletion.
func (c *accountServiceClient) PreviewAccountDeletion(ctx context.Context, req *connect.Request[v1.PreviewAccountDeletionRequest]) (*connect.Response[v1.PreviewAccountDeletionResponse], error) {
	return c.previewAccountDeletion.CallUnary(ctx, req)
}

// DeleteAccount calls reliant.v1.AccountService.DeleteAccount.
func (c *accountServiceClient) DeleteAccount(ctx context.Context, req *connect.Request[v1.DeleteAccountRequest]) (*connect.Response[v1.DeleteAccountResponse], error) {
	return c.deleteAccount.CallUnary(ctx, req)
}

// AccountServiceHandler is an implementation of the reliant.v1.AccountService service.
type AccountServiceHandler interface {
	// PreviewAccountDeletion reports what DeleteAccount would destroy, without
	// destroying anything. The UI shows these counts in the confirmation dialog
	// so the user sees the actual scale of what they are about to lose rather
	// than a generic warning.
	PreviewAccountDeletion(context.Context, *connect.Request[v1.PreviewAccountDeletionRequest]) (*connect.Response[v1.PreviewAccountDeletionResponse], error)
	// DeleteAccount permanently removes every row reliant holds for the caller.
	// Irreversible: there is no soft-delete and no undo.
	//
	// The caller must echo their own email address in confirm_email. This is a
	// deliberate speed bump on an unrecoverable action, and it is verified
	// server-side against the JWT's email claim — a client that skips its own
	// dialog still cannot delete by accident.
	DeleteAccount(context.Context, *connect.Request[v1.DeleteAccountRequest]) (*connect.Response[v1.DeleteAccountResponse], error)
}

// NewAccountServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAccountServiceHandler(svc AccountServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	accountServiceMethods := v1.File_reliant_v1_account_proto.Services().ByName("AccountService").Methods()
	accountServicePreviewAccountDeletionHandler := connect.NewUnaryHandler(
		AccountServicePreviewAccountDeletionProcedure,
		svc.PreviewAccountDeletion,
		connect.WithSchema(accountServiceMethods.ByName("PreviewAccountDeletion")),
		connect.WithHandlerOptions(opts...),
	)
	accountServiceDeleteAccountHandler := connect.NewUnaryHandler(
		AccountServiceDeleteAccountProcedure,
		svc.DeleteAccount,
		connect.WithSchema(accountServiceMethods.ByName("DeleteAccount")),
		connect.WithHandlerOptions(opts...),
	)
	return "/reliant.v1.AccountService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AccountServicePreviewAccountDeletionProcedure:
			accountServicePreviewAccountDeletionHandler.ServeHTTP(w, r)
		case AccountServiceDeleteAccountProcedure:
			accountServiceDeleteAccountHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAccountServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAccountServiceHandler struct{}

func (UnimplementedAccountServiceHandler) PreviewAccountDeletion(context.Context, *connect.Request[v1.PreviewAccountDeletionRequest]) (*connect.Response[v1.PreviewAccountDeletionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("reliant.v1.AccountService.PreviewAccountDeletion is not implemented"))
}

func (UnimplementedAccountServiceHandler) DeleteAccount(context.Context, *connect.Request[v1.DeleteAccountRequest]) (*connect.Response[v1.DeleteAccountResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("reliant.v1.AccountService.DeleteAccount is not implemented"))
}
