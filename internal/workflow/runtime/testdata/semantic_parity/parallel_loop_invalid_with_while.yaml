name: parallel_loop_invalid_with_while
workflow_yaml: |
  name: invalid-parallel-loop-with-while
  apiVersion: "1.0"
  entry: [implement_all]
  inputs:
    components:
      type: array
  nodes:
    - id: implement_all
      type: loop
      parallel: true
      items: "{{inputs.components}}"
      while: "iter.iteration < 3"
      ref: builtin://agent
  edges: []
canonical_workflow_ref: test://invalid-parallel-loop-with-while

core:
  use_workflow_loader: false
  expected_contracts: []

validation:
  should_pass: false
  use_workflow_loader: false
  error_contains: "parallel loop cannot use 'while'"

runtime:
  checks: []