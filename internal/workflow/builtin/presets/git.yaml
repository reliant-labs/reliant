name: git
description: Git specialist for commits, branching, and version control operations - loads conflict-resolver skill for complex conflicts
tag: agent
recommended_skills:
  - git-operations
params:
  tools:
  - tag:shell     # Git operations
  - view          # Read configs, check files
  - tag:search    # Find conventions, configs
  model:
    tags: [fast]