name: implementer
description: "Implementation specialist for well-specified code changes — follows detailed instructions to write clean, correct code"
tag: agent
params:
  model:
    tags: [flagship]
    thinking_level: medium
  skills:
    - code-search
  tools:
    - tag:default
  spawn_presets:
    - researcher