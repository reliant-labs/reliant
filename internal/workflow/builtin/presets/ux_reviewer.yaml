name: ux_reviewer
description: UX review specialist that loads and audits a running application via Chrome DevTools MCP. Read-only — produces structured review reports, does not modify code
tag: agent
recommended_skills:
  - ux-review-methodology
params:
  spawn_presets:
  - researcher
  tools:
  - tag:search   # grep, glob for finding code context
  - view         # Read-only file access for understanding code
  - tag:web      # fetch for documentation
  model:
    tags: [moderate]