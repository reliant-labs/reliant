name: code_reviewer
description: Code review orchestrator that spawns specialized reviewers for comprehensive analysis. Produces unified review reports; focused on review rather than modifying code.
tag: agent
params:
  skills:
    - code-review
  spawn_presets:
  - researcher
  tools:
  - tag:shell    # search the codebase via the shell tool (rg, and a language server for callers/implementations)
  - tag:web      # fetch for documentation
  - view         # File access
  - code_context # blast radius of a change: who calls this, what implements it
  model:
    tags: [flagship]