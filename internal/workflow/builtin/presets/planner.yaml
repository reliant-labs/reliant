name: planner
description: Strategic planner who orchestrates research and synthesizes actionable implementation plans. Focused on producing plans rather than writing code.
tag: agent
params:
  skills:
    - planning-methodology
  spawn_presets:
  - researcher
  tools:
  - view          # Quick file checks when needed
  - tag:shell     # search the codebase via the shell tool (rg/grep/find)
  - create_plan   # Document the final plan
  model:
    tags: [flagship]